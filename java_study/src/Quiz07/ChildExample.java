package Quiz07;

public class ChildExample {

	public static void main(String[] args) {
		Child child = new Child();
		// 상속관계에 있으면 자식객체를 생성할 때 부모객체를 생성한다.
		//  => 부모클래스의 생성자가 먼저 실행된다. => 매개변수가 없는 생성자가 우선이 된다.
		
		
	}

}
